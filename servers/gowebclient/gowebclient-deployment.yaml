# Filename: gowebclient-deployment.yaml
# Created: Wednesday May 23, 2018
# Modified: 
# Author: J. Benjamin Leeds
# License: None
# Purpose: Kubernetes Deployment YAML for Go Client Web Server

# Deployment Instructions
# Create and update deployment: kubectl apply -f gowebclient-deployment.yaml --record

apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "go-client-server-deployment"
  namespace: "default"
  labels:
    app: "go-client-server-app"
    tier: frontend
    environment: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "go-client-server-app"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: "go-client-server-app"
    spec:
      containers: # inside spec -> containers list
        - name: go-client-server-image # 1st element in a list inside containers
          image: us.gcr.io/airliftnw-uw/go-client-server-image:0.1
          ports:
            - containerPort: 80
              protocol: TCP


#              
#          env:
#            - name: ENVIRONMENT
#              value: "kubernetes"
#          volumeMounts:
#            - name: crewjam-secret-volume # mount volume specified below
#              mountPath: /etc/crewjam-secret-volume # make available at this path
#      volumes:
#        - name: crewjam-secret-volume # creates volume with this name
#          secret:
#            secretName: crewjam-test-elevate-airliftnw-org-letsencrypt-generic # add secret to vol