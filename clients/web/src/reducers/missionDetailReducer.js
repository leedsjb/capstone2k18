const initialState = {
    pending: false,
    error: null,
    data: []
};

const missionDetailReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default missionDetailReducer;
