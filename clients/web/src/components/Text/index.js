import styled from "styled-components";
import {
    space,
    color,
    fontSize,
    fontWeight,
    textAlign,
    lineHeight
} from "styled-system";

const Text = styled.p`
    ${space}
    ${color}
    ${fontSize}
    ${fontWeight}
    ${textAlign}
    ${lineHeight}
`;

export default Text;
